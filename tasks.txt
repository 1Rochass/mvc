 - Warning Соблюдать правильную запись путей.
	- Перед контроллером или бандлом ставить слеш / -> /admin/Login/
	- Название контроллера писать с заглавной буквы

 + Пишем главный класс
	+ kernel
	Session::sessionStart(); 
	Route::routeStart();
	Verification::verificationStart();

 + Сделать Twig для внутреннего пользования фреймворка глобальным
	- verification.php - проблемы с твигом 
 
 + Логин
	+ разрешить заходить на admin/login
	+ make class Login
	+ make view login
	+ make model login

	+ после удачного логирования обращатся к Session::sessionMakeVariable($user)
	 	и передавать аргумент с массивом $user(данные о пользователе)
	+ если возврещается true Header("Location:   главгая страница админ панели")
 
 - БД 
	- Приходится в каждой модели прописывать соединение с БД
	- sql injection

 - Автозагрузка классов 

 - При введении не правильного адреса появляется Ошибка "Страница localhost не работает"
	
	- Создать сборщик ошибок класс Error.php
	- подключаем его в bootstrap.php
	- И там где нужно уловить ошибку:

			if{...}
			else {
				Route::$Error[] = "Error text"; 
			}

	- в Errro.php должен быть реализован вывод страницы 404
	- И запускаем сборщик в Kernel.php Error:error();
	
		

